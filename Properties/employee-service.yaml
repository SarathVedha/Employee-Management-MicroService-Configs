server:
  port: 8081

spring:
#  application:
#    name: EMPLOYEE-SERVICE
  datasource:
    url: jdbc:postgresql://localhost/employee_db
    username: postgres
    password: Rockers12$
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
#  config:
#    import: optional:configserver:http://localhost:8888

# http://localhost:8080/employee-service/actuator/refresh
test:
  config:
    server: Message From Properties Updated MQ Bus

management:
  endpoint:
    health:
      show-details: always
    shutdown:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
  info:
    env:
      enabled: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

info:
  app:
    name: Employee Service
    description: Employee Service REST CURD APIs
    environment: ${spring.profiles.active}
    version: v1.0
